import './index.css';
import 'assets/css/font-awesome.css';
import pub from 'util/public.js';

//返回一个字符串的所有不同顺序组合数组
// const armu = str => {
//     if(str.length <= 2){
//         return str.length === 2 ? [str, str[1] + str[0]] : [str];
//     }

//     return str.split('').reduce((acc, letter, i) => {
//         return acc.concat(armu(str.slice(0, i) + str.slice(i + 1)).map(val => letter + val));
//     }, []);
// }

// const res = armu('abcde');
// console.log(res);
//-------------------------------------------------------------------------------------------------
//计算数组数字的平均数
// const arr = [1, 2, 3, 4, 5, 6, 7];
// const result = arr.reduce((acc, val) => {
//     return acc += val;
// }, 0) / arr.length;
// console.log(result);
//-------------------------------------------------------------------------------------------------
//每个单词首字母大写
// const str = 'zhangqi zhaojinge freddie';
// const result = str.replace(/\b[a-z]/g, match => match.toUpperCase());
// console.log(result);
//-------------------------------------------------------------------------------------------------
//检查回文
// const str = 'taco cat';
// function palindrome(str){
//     const s = str.toLowerCase().replace(/[\W_]/g, '');
//     return s === s.split('').reverse().join('');
// }
// console.log(palindrome(str));
//-------------------------------------------------------------------------------------------------
//计数数组中值的出现次数
// const arr = [1,2,3,1,1];
// const fn = (arr, val) => arr.reduce((acc, item) => item === val ? acc + 1 : acc + 0 , 0);
// console.log(fn(arr, 2));
//-------------------------------------------------------------------------------------------------
//


//-------------------------------------------------------------------------------------------------

//-------------------------------------------------------------------------------------------------

//-------------------------------------------------------------------------------------------------

//-------------------------------------------------------------------------------------------------

//-------------------------------------------------------------------------------------------------

//-------------------------------------------------------------------------------------------------

//-------------------------------------------------------------------------------------------------

//-------------------------------------------------------------------------------------------------

//-------------------------------------------------------------------------------------------------
//-------------------------------------------------------------------------------------------------




